base_uri:             http://github.zrok.quigley.com:8081
port:                 8081
cookie_domain:        "github.zrok.quigley.com"
cookie_max_age:       86400  # 60 * 60 * 24
cookie_expires:       "24h"
refresh_every:        "10m"
before_expiry:        "5m"

issuer_url:           "https://github.com"
scopes:
  - openid
pkce:                 true
callback_path:        "/auth/callback"
